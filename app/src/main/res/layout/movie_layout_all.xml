<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/movies_margin">

    <ImageView
        android:id="@+id/movie_image_all"
        android:layout_width="match_parent"
        android:layout_height="@dimen/movie_image_height"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:contentDescription="@string/movie_picture"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/movie_name"
        android:layout_width="match_parent"
        android:layout_height="@dimen/movie_textview_height"
        android:background="@color/transparent_grey"
        android:gravity="center"
        android:textSize="@dimen/movie_title_size"
        android:textColor="@color/dark_blue"
        android:textStyle="bold"
        android:typeface="monospace"
        android:shadowColor="@color/light_blue"
        android:shadowRadius="10"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="@+id/movie_image_all"/>

</androidx.constraintlayout.widget.ConstraintLayout>